"""项目入口文件。

Step 1 的 MVP 目标非常小：
只验证项目骨架和 uv 运行链路是否正常，不接入数据库、TUI 或 LangChain。
"""


def main() -> None:
    """启动项目。

    当前步骤只打印固定启动信息，方便验证工程初始化是否成功。
    """
    print("LangChain Chat 项目已启动")


if __name__ == "__main__":
    main()
