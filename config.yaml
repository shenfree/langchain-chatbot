# LangChain Chat 全局配置文件
# Step 3 开始，storage.sqlite.path 会被 SQLite 初始化脚本读取。

app:
  name: langchain-chat
  version: 0.1.0
  env: development

model:
  default_provider: openai
  default_model: gpt-4o-mini

storage:
  type: sqlite
  sqlite:
    path: data/sqlite/app.db

tui:
  theme: default
